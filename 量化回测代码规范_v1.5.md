# 量化回测代码规范（生产最小可用版）

版本：v1.5r4  
适用范围：股票/指数/行业等**日频及以下**回测系统  
目标：保证结果可复现、无前视偏差、可交易落地、可审计、可持续维护

> **适用边界**：本规范适用于 A 股（含主板、创业板、科创板、北交所）的股票/指数/行业策略，频率为日频及以下（周/月/季度调仓）。以下场景**不在**本规范覆盖范围内，须制定专项规范后方可使用：
> - 高频 / tick 级策略
> - 期权、期货及其他衍生品策略
> - 跨市场（港股通、美股 ADR）策略
> - 场外衍生品 / 结构化产品

---

### 变更日志

| 版本 | 日期 | 变更内容 | 责任人 |
|------|------|----------|--------|
| v1.0 | 2026-02-15 | 初始版本，覆盖数据层、回测严谨性、缓存、性能、输出、测试 | — |
| v1.1 | 2026-02-16 | 新增交易执行（§5）、风险归因（§8）、稳健性防控（§11.3） | — |
| v1.2 | 2026-02-17 | 修复编号错误；新增复权规范（§3.5）、基准定义（§2.3）、统计检验（§11.4）、调仓执行窗口（§5.5）；补充数据清洗规则（§3.6） | — |
| v1.3 | 2026-02-17 | 统一 t-stat 门槛 2.0；复权跳变 ±35%；成本模型参考值（§5.3）；调仓明细格式（§10.1）；策略容量评估（§8.4）；术语表（附录A）；Code Review（§12.1）；block bootstrap 明确化（§11.4）；研究→实盘交接（§13） | — |
| v1.4 | 2026-02-17 | 印花税更正为千0.5；新增随机种子（§2.4）、无风险利率（§2.5）、数据源声明（§2.6）、公告缓冲期（§4.1）、行业分类历史版本（§3.7）、指数成分股历史（§3.8）、停牌强制移出（§4.4）、部分成交（§10.1）、缓存过期（§6.3）、HHI 集中度（§8.1）、归因残差校验（§8.3）、日度持仓快照（§10.3）、样本外检验（§11.5）、策略衰减监控（§11.6）、极端区间专项（§11.7）；补充规范治理（§15）、P1 豁免模板（附录B） | — |
| v1.4r1 | 2026-02-17 | 修正：成本表格数值矛盾、佣金分低佣/零售两档（§5.3.1）；部分成交权重公式（§10.1）；随机种子推荐 Generator API（§2.4）；停牌检查窗口明确化（§4.4）；跳变阈值分板块映射表（§3.5）；t-stat 门禁增加替代条件（§12.2）；新增基准收益类型（§2.3）、闲置资金假设（§4.4）；对账告警分层（§13.4）；补充 2024 极端区间（§11.7）；重新验证定义明确化（§15.3） | — |
| v1.4r2 | 2026-02-17 | §3.6 异常阈值与 §3.5 分板块体系统一（P0）；版本号同步 v1.4r1；治理章节去重引用 §15；direction 细分 NEW/ADD/REDUCE/EXIT（§10.1-10.2）；分红再投资成本说明（§5.3.2）；OOS 低频策略 ≥2 年建议（§11.5）；容量阈值 CAPACITY_SHARPE_DECAY_THRESHOLD 可配置（§8.4）；Checklist 补充 BENCHMARK_TYPE、CASH_RETURN_ASSUMPTION（§14） | — |
| v1.4r3 | 2026-02-17 | 停牌强制移出增加复牌卖出价假设 SUSPEND_EXIT_PRICE（§4.4）；成本模型增加单笔最低佣金 MIN_COMMISSION_PER_TRADE（§5.3.1） | — |
| v1.4r4 | 2026-02-17 | fail_reason 枚举补充 SUSPEND_EXIT（§10.1）；run_metadata 补充回测描述字段（§9）；code_review_sign.json 增加完整模板与字段定义（§12.1） | — |
| v1.4r5 | 2026-02-17 | §14 Checklist 补充 SUSPEND_EXIT_PRICE（🔴）和 MIN_COMMISSION_PER_TRADE（🟡）检查项 | — |
| **v1.5** | **2026-02-17** | **正式定稿版**。修复 §14 SUSPEND_EXIT_PRICE emoji 渲染（P0）；§4.4 停牌移出"涨跌停"→"跌停或无法成交"、ADV 窗口明确为"停牌前 20 个交易日"；§9 尾部风险指标增加 Calmar Ratio | — |
| **v1.5r1** | **2026-02-17** | 文件名同步 v1.5；豁免模板统一为 `waiver_log.json`（P0/P1 通用，增加 `priority_level` 字段），修复 §12.2 P0 门禁引用 P1 模板的语义矛盾（P0）；§4.4 停牌回溯修正增加时点与会计处理定义（P0） | — |
| **v1.5r2** | **2026-02-17** | §4.4 修复三阶段会计重复计量（成交日仅结算 vs 前一估值价差额）（P0）；§4.4 权重处理拆分目标权重/实际权重双轨（P0）；§4.4 新增停牌期间退市边界处理（P0）；§10.1 direction 统一为 5 值枚举 NEW/ADD/HOLD/REDUCE/EXIT（P1）；§9 补充 OOS 字段交叉引用（P2）；§11.7 新增 2025 极端区间占位（P2）；§2.5 固定利率示例 3%→1.5%（P2） | — |
| **v1.5r3** | **2026-02-17** | §4.4 `rebalance_log` 标记按场景分流 SUSPEND_EXIT / DELISTED_DURING_SUSPEND（P0）；§14 极端区间检查项改为引用 §11.7（P1）；§15.1 新增版本命名规则（P1）；§10.3 持仓快照增加 `status` 字段（P2）；附录 A 补充 SUSPEND_EXIT / DELISTED_DURING_SUSPEND 术语（P2） | — |
| **v1.5r4** | **2026-02-17** | §4.4 权重重分配溢出处理（超限计入现金）（P1）；§11.1 新增 DELISTED_DURING_SUSPEND 结算与 PENDING_EXIT 快照两条单测（P1）；§13.2 信号 direction 显式引用 §10.1 枚举（P1）；§15.1 主版本升级判定条件（P2） | — |

---

### 规范治理

> 详见 §15。修订权限、审批流程、定期 Review、版本归档等治理要求统一在 §15 中定义，此处不再重复。

---

## 规则分级说明

本规范所有条目按影响程度分为三级，团队执行时按级别区分处理方式：

| 级别 | 标记 | 含义 | 处理方式 |
|------|------|------|----------|
| **P0 阻塞** | 🔴 | 违反将导致回测结论不可信 | 不通过则禁止提交/发布 |
| **P1 强烈建议** | 🟡 | 不影响正确性但影响实盘可信度 | 允许临时豁免，须填写豁免记录（见附录B） |
| **P2 最佳实践** | 🟢 | 提升代码质量与维护效率 | 鼓励但不强制 |

---

## 1. 项目结构规范

### 1.1 推荐目录结构 🟢

```text
project/
  config.py                 # 参数与环境配置
  data_validation.py        # schema/coverage校验
  download_data.py          # 数据下载与增量更新
  strategy.py               # 策略逻辑与回测引擎
  runtime_utils.py          # 运行上下文、日志、元数据
  factors/                  # 因子计算模块（多因子策略适用）
  tests/                    # 单元测试与回归测试
  notebooks/                # 探索性分析（仅研究用，禁止作为生产入口）
  data/                     # 原始数据与缓存
  output/                   # 回测结果(run目录)
```

### 1.2 强制要求 🔴
- 配置、数据、策略、回测、输出必须分层，禁止单文件"大脚本"。
- 所有可复现实验必须输出独立 run 目录。
- 任何关键逻辑修改后，必须可通过自动化测试验证。
- **Jupyter Notebook 仅用于探索性分析**，禁止作为生产回测入口。Notebook 中的关键结论须迁移到 `.py` 文件并纳入测试。

---

## 2. 配置与环境规范

### 2.1 配置管理 🔴
- 参数集中在 config.py，禁止散落在业务代码中。
- API 密钥仅允许环境变量读取，禁止明文写入仓库。
- 必须设置：
  - 回测区间
  - 交易成本（参见 §5.3 参考值）
  - 调仓频率
  - 缓存版本号
  - 校验严格模式开关

### 2.2 环境隔离 🟡
- 必须使用虚拟环境。
- 依赖写入 requirements.txt。
- 生产运行前需执行：
  - 语法检查
  - 单元测试

### 2.3 基准定义 🔴
- 必须在 config.py 中显式声明 `BENCHMARK`（如 `000300.SH` / `000905.SH` / 等权全 A）。
- 所有超额收益、信息比率、归因拆解、风险暴露均以此基准为锚。
- 禁止在不同模块中隐式使用不同基准。
- 若策略适用多基准比较，须在 config 中定义 `BENCHMARK_LIST`，输出中逐一列示。
- 必须声明基准收益类型 `BENCHMARK_TYPE`：`"total_return"`（全收益指数，含分红再投资）或 `"price_return"`（价格指数）。A 股全收益指数与价格指数年化差异可达 1.5~2.5%，默认使用**全收益指数**。

### 2.4 随机种子管理 🔴
- 若策略或检验流程涉及任何随机过程（bootstrap、随机森林、随机初始化、蒙特卡洛模拟等），必须在 config.py 中设置 `RANDOM_SEED`。
- 所有随机操作必须引用此种子（推荐使用 Generator API）：
  ```python
  import numpy as np
  # 推荐：Generator API（NumPy ≥ 1.17），避免全局状态被第三方库意外修改
  rng = np.random.default_rng(config.RANDOM_SEED)
  # 兼容写法（旧代码迁移期可用，但须注意全局状态风险）
  # np.random.seed(config.RANDOM_SEED)
  ```
- 禁止在代码中使用无种子的随机调用。
- 这是**可复现性的必要条件**：相同输入 + 相同种子 = 相同输出。

### 2.5 无风险利率定义 🔴
- 必须在 config.py 中显式声明 `RISK_FREE_RATE`，标注来源和更新频率。
- 推荐选择（任选其一并在 config 中注明）：

| 选项 | 说明 | 适用场景 |
|------|------|----------|
| 一年期国债到期收益率 | 中债估值中心数据 | 学术研究、报告展示 |
| 一年期 SHIBOR | 上海银行间同业拆借利率 | 机构内部评估 |
| 固定值（如 1.5%） | 简化假设，须标注"非实际利率" | 快速原型、教学 |

- Sharpe Ratio 公式中的 $R_f$ 必须使用此处声明的值，禁止各模块自行假设。
- 若使用动态利率（按日/月变化），须在 `data/` 中维护利率时间序列文件。

### 2.6 数据源声明 🟡
- 必须在 config.py 或 run_metadata.json 中记录数据来源（如 `Tushare Pro` / `Wind` / `聚宽` / `RiceQuant`）。
- 不同数据源的字段定义、数据质量、更新时效可能存在差异，切换数据源后须重新跑全量回测验证。
- 推荐在 config 中维护 `DATA_SOURCE` 和 `DATA_SOURCE_VERSION` 字段。

---

## 3. 数据层规范

### 3.1 字段一致性（Schema）🔴
- 每个数据集必须定义 required 字段。
- 下载后立即校验 required 字段，缺失必须 fail-fast。
- 接口字段改名或缺失时，必须有兼容层或显式报错。

### 3.2 覆盖率（Coverage）🔴
- 关键字段必须有覆盖率阈值（如 95%/99%）。
- 每次运行输出 coverage 报告（json）。
- 低于阈值可配置为 warning 或 error（严格模式下 error）。

### 3.3 日期与类型统一 🔴
- 业务主链路统一使用 YYYYMMDD 字符串（推荐）。
- 时间列在入库或加载后统一 dtype，禁止同列 str/datetime 混用。
- 交易日历、调仓日、公告日比较必须使用同一类型。

### 3.4 排序与唯一性 🔴
- 交易日历必须升序。
- 回测收益序列必须升序、无重复索引。
- 同一主键组合必须去重（如 ts_code + ann_date + end_date）。

### 3.5 复权与公司行动处理 🔴
- **复权方式**：必须在 config.py 中显式声明使用前复权、后复权或不复权，不允许中途切换。
  - 推荐：计算收益率使用**后复权价格**，或直接使用数据源提供的复权因子。
  - 禁止：用不复权收盘价直接计算持有期收益率。
- **公司行动（Corporate Actions）**：
  - 送转/拆合股：必须使用复权因子修正历史价格或持仓股数。
  - 配股：必须决定是否跟配，并在 config 中记录处理规则。
  - 分红除息：现金分红必须显式处理（再投资 or 计入现金），不得静默忽略。
- **校验**：在载入日线行情后，必须检查复权后价格无负值、无日间跳变超过 **±35%**（排除真实涨跌停）的异常。
  - 可在 config 中通过 `ADJ_PRICE_JUMP_THRESHOLD` 按板块覆盖，推荐分板块设置：

  | 板块 | 涨跌停幅度 | 推荐跳变阈值 | 说明 |
  |------|-----------|-------------|------|
  | 主板 | ±10% | ±15% | 10% + 5% 容差 |
  | 创业板 / 科创板 | ±20% | ±25% | 20% + 5% 容差 |
  | 北交所 | ±30% | ±35%（默认值） | 30% + 5% 容差 |
  | ST / *ST | ±5% | ±8% | 5% + 3% 容差 |

  - 超过阈值的记录须写入 `data_cleaning_log.json`。

### 3.6 数据清洗规则 🟡
- **行情异常值**：
  - 非涨跌停日出现涨跌幅超过 §3.5 分板块跳变阈值的记录，须标记并记入清洗日志（主板 > ±15%、创科 > ±25%、北交所 > ±35%、ST > ±8%）。
  - 成交量为 0 但涨跌幅 ≠ 0 的记录，须标记为可疑。
  - 开盘价/最高价/最低价/收盘价逻辑关系（H ≥ max(O,C)，L ≤ min(O,C)）必须校验。
- **财务数据异常值**：
  - 负净资产：须标记，选股逻辑中显式决定是否排除。
  - 同一股票同一报告期的多条修正公告：取最晚 ann_date 的记录（即最新修正值）。
  - 财务数据单位（万元 / 元 / 亿元）须在 schema 中强制定义并校验统一。
- **清洗记录**：所有清洗操作必须输出 `data_cleaning_log.json`，记录被修改/排除的行数及规则。

### 3.7 行业分类历史版本 🔴
- 行业分类（中信 / 申万等）每年有调整，**必须使用历史时点的行业映射**，而非当前最新版本。
- 要求：
  - 下载并存储行业分类的历史版本表（含生效日期）。
  - 归因、行业暴露、行业集中度计算时，须按 `as_of_date` 查找对应版本的行业映射。
  - 禁止用当前行业分类回溯历史，否则行业归因结果会失真。
- 在 config 中声明 `INDUSTRY_CLASSIFICATION`（如 `SW_2021` / `CITIC_2020`）及版本管理方式。

### 3.8 指数成分股历史 🔴
- 若策略以指数为基准或使用指数成分股构建股票池，**必须使用历史时点的成分股列表**，而非当前成分股。
- 当前成分股回溯历史本质上也是幸存者偏差。
- 要求：
  - 下载并存储指数成分股的历史变更记录（含纳入日期、剔除日期）。
  - 基准收益计算时，按 `as_of_date` 使用当时的成分股列表和权重。

---

## 4. 回测严谨性规范

### 4.1 PIT 原则（Point-In-Time）🔴
- 财务/公告数据必须按发布日可得性使用。
- 调仓时只能使用"调仓日及以前"可观测数据。
- 严禁用报告期 end_date 直接代表可得时间。
- **公告日缓冲期**：数据供应商（Tushare / Wind 等）收录公告通常有 1~3 天延迟。必须在 config 中设置 `DATA_AVAILABILITY_LAG`（推荐默认 1 天），实际可得日 = ann_date + lag。
  - 选股过滤条件相应调整为：`ann_date + DATA_AVAILABILITY_LAG <= rebal_date`。
  - 该参数在参数敏感性分析中须测试 0/1/2/3 天四档。

### 4.2 前视偏差防控 🔴
- 任何选股条件需显式过滤：
  - ann_date <= rebal_date（考虑 §4.1 缓冲期后为 ann_date + lag <= rebal_date）
  - signal_date <= rebal_date
- 回测代码必须有前视偏差单测。

### 4.3 幸存者偏差防控 🔴
- 股票池按历史时点构建：
  - list_date <= as_of_date
  - delist_date > as_of_date
- 禁止用"当前存活股票列表"回溯历史。
- 指数成分股须使用历史时点列表（参见 §3.8）。

### 4.4 交易假设 🔴
- 必须显式设定交易成本。
- 缺失收益（停牌）处理规则必须固定并可审计（如填 0）。
- 若策略依赖涨跌停可交易性，需增加可交易过滤规则。
- **停牌强制移出**：停牌超过 `MAX_SUSPEND_DAYS` 个交易日（config 可配置，推荐默认 10 个交易日）的标的，须强制从持仓中移出，并在 run.log 中记录移出原因。
  - **检查时点**：推荐每日检查（尤其是低频策略），触发时生成**中间调仓指令**仅做单标的移出操作；若仅在调仓日检查，须在 run_metadata 中输出"非调仓日累计停牌超限但未处理"的标的列表，供审计。
  - 被移出标的的**目标权重**须在触发移出决策当日清零，按比例重分配给剩余持仓（用于后续调仓信号计算）；重分配后若触发 §8.1 单票或行业权重上限，溢出部分计入现金仓位并在 run.log 中记录。**实际权重**因无法物理卖出而保持不变，标记为"待移出"状态，直至实际成交日归零。
  - 停牌期间"填 0"仅适用于 `MAX_SUSPEND_DAYS` 以内的短期停牌。
  - **移出交割价格假设**：停牌股无法即时卖出，须在 config 中声明 `SUSPEND_EXIT_PRICE`（推荐默认 `RESUME_VWAP`）。具体规则：
    - `RESUME_VWAP`（推荐）：复牌首日 VWAP。
    - `RESUME_OPEN`：复牌首日开盘价。
    - `RESUME_CLOSE`：复牌首日收盘价。
    - 若复牌首日**跌停**（或虽非跌停但连续竞价阶段无法成交）且成交量极低（成交额 < 该股**停牌前** 20 个交易日日均成交额的 10%），视为不可成交，顺延至首个可正常成交日。
    - **回溯修正时点与会计处理**：移出期间（停牌决策日→实际成交日）该标的的净值变动须按以下规则处理：
      - **停牌期间**（决策日→复牌日前一日）：该标的每日收益记为 0（价格冻结），实际权重保持不变但标记为"待移出"状态。
      - **复牌日（含）起至实际成交日前一日**：按每日实际收盘价逐日盯市（mark-to-market），每日估值变动计入当日组合收益。
      - **实际成交日**：以成交价与**前一估值日收盘价**的差额确认当日盈亏，计入当日组合收益。禁止以"成交价 vs 停牌前成本"计算差额（否则与阶段 2 盯市重复计量）。
      - 禁止以停牌前价格假设卖出（即禁止"停牌日即成交"假设）。
      - 上述处理须在 `run.log` 中逐标的记录各阶段的起止日期和价格。
    - **停牌期间退市处理**：若标的在停牌期间触发退市（强制退市、重大违法等），按以下规则结算：
      - 若进入退市整理期：以退市整理期首日开盘价结算；
      - 若直接摘牌（无退市整理期）：以最后一个交易日收盘价结算，亏损全额计入组合收益。
      - 在 `fail_reason` 中标记 `DELISTED_DURING_SUSPEND`，须在 `run.log` 中记录退市公告日期和结算价格。
    - 在 `rebalance_log.csv` 中须按场景标记 `fail_reason`：正常复牌卖出标记 `SUSPEND_EXIT`，停牌期间退市标记 `DELISTED_DURING_SUSPEND`，并记录实际成交/结算日期和价格。
- **闲置资金收益假设**：因停牌移出、部分成交、现金仓位等导致的未满仓资金，须在 config 中声明 `CASH_RETURN_ASSUMPTION`（推荐默认 0，保守处理；或按活期存款利率 0.2% 年化）。禁止回测中静默忽略现金收益/成本。

---

## 5. 交易执行可实现性规范

### 5.1 交易制度约束（A股）🔴
- 必须显式定义并在代码中执行：
  - T+1（当日买入次日可卖）
  - 最小交易单位（100股整数倍）
  - 涨跌停不可成交处理
  - 停牌不可成交处理
- 若为其他市场（港股/美股/期货），必须单独定义制度参数，不得复用 A 股默认规则。

### 5.2 成交价与执行时点 🔴
- 必须明确回测撮合价：开盘价/均价/收盘价之一，不得混用。
- 若信号在收盘后生成，默认下一交易日开盘执行。
- 若信号在盘中生成，必须说明是否允许同日成交及延迟机制。

### 5.3 成本模型分层 🔴

#### 5.3.1 A 股成本模型参考值

以下为截至 2026 年的 A 股通用参考值（**印花税已按 2023 年 8 月 28 日起的减半税率更新**），须在 config.py 中逐项可配置，策略上线前按实际券商费率核实更新：

| 成本项 | 方向 | 参考值 | 说明 |
|--------|------|--------|------|
| 佣金（低佣） | 单边 | 万 1.25 | 大资金 / 自营通道，含规费 |
| 佣金（普通零售） | 单边 | 万 2.5 | 一般散户账户，含规费 |
| 印花税 | 卖出单边 | **千 0.5（0.05%）** | 仅卖出征收（2023-08-28 起减半） |
| 过户费 | 单边 | 万 0.1 | 沪深均征收 |
| **基础层合计（低佣）** | **单次买卖往返** | **约 7.7bp** | 买入 1.35bp + 卖出 6.35bp |
| **基础层合计（零售）** | **单次买卖往返** | **约 10.2bp** | 买入 2.6bp + 卖出 7.6bp |

> **低佣基准**（单边万 1.25，适用于大资金 / 自营）：  
> 买入成本：佣金 1.25bp + 过户费 0.1bp = **1.35bp**  
> 卖出成本：佣金 1.25bp + 过户费 0.1bp + 印花税 5.0bp = **6.35bp**  
> 往返合计：1.35 + 6.35 = **7.7bp**  
>  
> **普通零售**（单边万 2.5，适用于一般账户）：  
> 买入成本：佣金 2.5bp + 过户费 0.1bp = **2.6bp**  
> 卖出成本：佣金 2.5bp + 过户费 0.1bp + 印花税 5.0bp = **7.6bp**  
> 往返合计：2.6 + 7.6 = **10.2bp**  
>  
> 实际费率因券商而异，**必须在 config 中按实际值配置**。参考值仅用于未配置时的 sanity check。
>
> **单笔最低佣金**：多数券商设有单笔最低佣金 **5 元**。当单笔交易金额 < 4 万元（按万 1.25 计）或 < 2 万元（按万 2.5 计）时，实际佣金率将显著高于上述参考值。config 中应增加 `MIN_COMMISSION_PER_TRADE`（默认 5.0 元），回测撮合时须取 `max(成交金额 × 佣金费率, MIN_COMMISSION_PER_TRADE)` 作为实际佣金。对持仓数量多（> 50 只等权）或小资金策略（< 1000 万），最低佣金的成本提升效应不可忽略。

| 增强成本项 | 参考值 | 适用场景 |
|------------|--------|----------|
| 滑点 | 单边 5~10bp | 日频调仓 / 中小盘 |
| 冲击成本 | 单边 5~20bp | 资金量 > 5000 万 / 成交占比 > 5% |
| **增强层合计** | **额外 20~60bp 往返** | 按策略资金量和标的流动性调节 |

#### 5.3.2 成本模型要求
- 基础层（佣金 + 印花税 + 过户费）为**最低必须项**。
- 增强层（滑点 + 冲击成本）为**实盘评估必须项**。
- 实盘评估必须至少给出"基础层"和"基础层 + 增强层"两套结果并对比。
- 若 §3.5 选择分红再投资，再投资买入须计入基础层买入成本（佣金 + 过户费，无印花税）。
- 冲击成本建模推荐使用 Almgren-Chriss 模型或简化的平方根模型：
  - $\text{Impact} = k \cdot \sigma \cdot \sqrt{\frac{V_{trade}}{V_{market}}}$
  - 其中 $k$ 为经验系数，$\sigma$ 为日波动率，$V_{trade}/V_{market}$ 为成交占比。
  - $k$ 值参考（按市值分段）：

| 市值档位 | $k$ 参考值 | 说明 |
|----------|-----------|------|
| 大盘（沪深300成分股） | 0.1 ~ 0.15 | 流动性充裕 |
| 中盘（中证500成分股） | 0.15 ~ 0.25 | 中等流动性 |
| 小盘（其余） | 0.25 ~ 0.50 | 流动性较差，成交占比敏感 |

### 5.4 流动性与可交易过滤 🟡
- 必须设定最小成交额或换手率阈值。
- 必须支持过滤 ST/*ST、退市整理期、上市未满 N 日标的（N 在 config 中可配置，推荐默认 60 个交易日）。
- 低流动性标的被过滤比例必须输出到 run_metadata。

### 5.5 调仓执行窗口 🔴
- 必须在 config.py 中定义调仓执行方式及窗口长度：
  - **单日执行**：如 T+1 日开盘价 / 收盘价。适用于持仓数量少（< 30 只）且流动性充足的策略。
  - **多日执行**：如 T+1 至 T+3 日 TWAP / VWAP。适用于持仓数量多（> 50 只）或资金量较大的策略。
- 撮合价格假设必须与执行窗口一致：
  - 若声明多日 TWAP，回测中必须按窗口内各日均价加权计算成交价，不得简化为 T+1 收盘价。
- 必须输出**执行窗口敏感性对比**：至少给出"T+1 开盘价"和"T+1~T+3 VWAP"两套结果，量化执行延迟对策略绩效的影响。
- 若调仓执行窗口内出现停牌/涨跌停，须按 §5.1 规则处理，并在 run_metadata 中记录未能成交的标的及金额。

---

## 6. 缓存与一致性规范

### 6.1 缓存版本机制 🔴
- 缓存必须包含元数据文件，至少记录：
  - cache_version
  - 数据签名（行数、日期范围、标的数、来源）
  - 生成时间
- 签名不一致或版本升级时，必须自动失效并重建。

### 6.2 缓存命中策略 🟡
- 回测宽表（如 trade_date x ts_code）可缓存。
- 命中缓存前必须验证签名。
- 缓存缺失/损坏时自动回退到重建流程。

### 6.3 缓存过期机制 🟡
- 即使签名一致，超过 `CACHE_EXPIRY_DAYS` 天（config 可配置，推荐默认 90 天）的缓存也应自动失效并重建。
- 目的：防止数据源端的静默修正（如财务数据追溯调整、行情数据修正）不被感知。
- 缓存元数据中须记录 `created_at` 时间戳，加载时与当前时间比对。

---

## 7. 性能规范

### 7.1 数据类型优化 🟢
- 数值列优先 float32（在精度允许前提下）。
- 高重复字符串列使用 category（如 ts_code）。

### 7.2 下载优化 🟢
- 批量按日期拉取，尽量减少请求次数。
- 支持增量下载，避免全量重复拉取。
- 限频参数支持自适应退避（错误变慢、成功恢复）。

### 7.3 计时与基准 🟢
- 必须提供关键阶段计时（读取、构建宽表、命中缓存）。
- 性能改造后需给出可复现对比数据。

---

## 8. 组合风险约束与归因规范

### 8.1 组合约束 🟡
- 必须支持并记录以下约束：
  - 单票权重上限
  - 单行业权重上限
  - 组合换手率上限（须注明计算口径：单边/双边，按市值/按股数）
  - 现金仓位下限/上限
- 任一约束触发时，必须在 run.log 记录触发原因与调整后权重。
- 建议引入**权重偏离缓冲区**（如 ±2%），减少不必要换仓，降低交易成本。
- **组合集中度**：必须输出 Herfindahl 指数（HHI = $\sum w_i^2$）和前 10 大持仓的权重占比。即使单票权重不超限，集中度指标也可能揭示隐性集中风险。
- 年化换手率参考警戒线：

| 策略频率 | 年化双边换手率警戒线 | 说明 |
|----------|---------------------|------|
| 低频（月/季调仓） | > 10 倍 | 超过须检查是否过度交易 |
| 中频（周调仓） | > 30 倍 | 成本吞噬风险显著 |
| 日频 | > 100 倍 | 几乎不可实盘，须极低成本通道 |

- run_metadata 中**必须输出年化换手率**，并标注是否超过预设阈值。

### 8.2 风险暴露控制 🟡
- 至少输出行业暴露、风格暴露（如市值/价值/动量）与基准偏离。
- 若策略为行业中性或市值中性，必须输出中性化前后对比。
- 团队内部须统一风险模型选择，并在 config 中通过 `RISK_MODEL` 声明（如 `BARRA_CNE6` / `CUSTOM`）。

### 8.3 归因与解释 🟡
- 必须提供：
  - 行业归因
  - 选股归因
  - 交易归因（成本拖累）
- 归因结果需与超额收益在统计上可对齐（允许小误差）。
- **归因残差校验**：行业归因 + 选股归因 + 交易归因的加总应近似等于总超额收益，年化残差不应超过 **±50bp**。超过须排查归因模型遗漏。

### 8.4 策略容量评估 🟡
- **必须在 run_metadata 中输出策略有效容量估计。**
- 评估方法：按以下资金档位分别模拟冲击成本退化，输出对应绩效指标：

| 资金档位 | 用途 |
|----------|------|
| 1000 万 | 个人/小型产品验证 |
| 5000 万 | 小型专户/FOF子策略 |
| 1 亿 | 中型专户 |
| 5 亿 | 大型产品可行性边界 |

- 冲击成本随资金量递增的建模：
  - 每只股票的成交占比 = 策略分配金额 / 该股当日成交额。
  - 成交占比 > 5% 的标的须单独标记。
  - **成交占比 > 10% 的标的须从组合中剔除并重新分配权重**，再计算剩余组合绩效（否则容量曲线不真实）。
  - 冲击成本随成交占比非线性增长（参见 §5.3 平方根模型）。
- 须输出**容量-年化收益曲线**和**容量-Sharpe 曲线**。
- 定义"有效容量上限"：Sharpe 降至无冲击成本时的 `CAPACITY_SHARPE_DECAY_THRESHOLD`（config 可配置，默认 0.7，即 70%）对应的资金量。

---

## 9. 输出与审计规范

每次回测 run 目录必须包含：
- run.log：结构化日志 🔴
- config_snapshot.json：参数快照 🔴
- run_metadata.json：核心指标摘要 🔴
- data_validation_report.json：schema/coverage 报告 🔴
- data_cleaning_log.json：数据清洗记录 🟡
- 日收益率、调仓记录（格式见 §10.1）、分年度绩效 🔴
- 日度持仓快照（格式见 §10.3）🟡
- 关键图表（净值、回撤、年度收益）🟡
- 缓存元数据（签名、版本、创建时间）🟡
- 约束触发统计与交易失败统计 🟡
- 统计检验报告（t-stat、p-value、置信区间）🔴

**run_metadata.json 必须包含的回测描述字段**：
- 回测起始日期（backtest_start_date）
- 回测结束日期（backtest_end_date）
- 调仓期数（n_rebalance_periods）
- 平均持仓数（n_holdings_avg）

**run_metadata.json 必须包含的尾部风险指标**：
- 最大回撤（MaxDD）
- 最大回撤持续天数（MaxDD Duration）
- CVaR 95%（条件在险价值）
- 最大连续亏损月数
- 月度胜率
- 盈亏比（Profit/Loss Ratio）
- Calmar Ratio（年化收益 / |MaxDD|）
- 年化换手率（含口径说明）
- HHI 集中度（平均值和最大值）
- 策略有效容量上限估计
- 数据源（DATA_SOURCE）

**run_metadata.json 中统计检验字段**：
- t_stat, p_value_nw, ci_95_lower, ci_95_upper, n_tests, p_value_adjusted
- 当 `n_tests = 1` 时，`p_value_adjusted = p_value_nw`（无需调整，但须显式赋值以保持字段完整性）

**run_metadata.json 中样本外检验字段**（详见 §11.5）：
- oos_start_date, oos_annual_return, oos_sharpe, oos_max_drawdown

---

## 10. 调仓明细与记录规范

### 10.1 调仓明细格式（rebalance_log.csv）🔴

每次调仓必须输出结构化明细文件 `rebalance_log.csv`，最低字段定义如下：

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| rebal_date | str(YYYYMMDD) | ✅ | 调仓决策日 |
| exec_date | str(YYYYMMDD) | ✅ | 实际执行日 |
| ts_code | str | ✅ | 标的代码 |
| direction | str | ✅ | NEW / ADD / HOLD / REDUCE / EXIT（5 值枚举，禁止使用 BUY/SELL） |
| target_weight | float | ✅ | 目标权重（0~1） |
| prev_weight | float | ✅ | 调仓前权重（0~1） |
| actual_weight | float | ✅ | 调仓后实际权重 |
| exec_price | float | ✅ | 撮合价格假设（元） |
| exec_type | str | ✅ | OPEN / CLOSE / VWAP / TWAP |
| traded | bool | ✅ | 是否完全成交 |
| fill_ratio | float | ✅ | 成交比例（0~1，1.0=完全成交，0=未成交） |
| fail_reason | str | ❌ | 未成交/部分成交原因（SUSPENDED / LIMIT_UP / LIMIT_DOWN / LOW_LIQUIDITY / PARTIAL_FILL / SUSPEND_EXIT / DELISTED_DURING_SUSPEND / 空值） |
| trade_amount | float | ✅ | 实际成交金额（元） |
| trade_shares | int | ✅ | 实际成交股数 |
| cost_bps | float | ✅ | 该笔交易总成本（bp） |
| industry | str | ❌ | 所属行业（按历史时点映射） |

**部分成交处理规则**（fill_ratio < 1.0 时）：
- 实际权重按 fill_ratio 比例调整：`actual_weight = prev_weight + (target_weight - prev_weight) × fill_ratio`。
  - 示例：某股从 5% 调至 10%，fill_ratio = 50%，则 actual_weight = 5% + 5% × 0.5 = **7.5%**。
  - 注意：该公式仅计算单标的权重调整，忽略其余持仓的再平衡效应。
- 未成交部分的资金按比例分配给同组其他已成交标的，或暂存为现金。
- 部分成交的标的和比例须在 run.log 中记录。

### 10.2 调仓汇总要求 🟡
- 每期调仓须在 run.log 中输出汇总统计：
  - 调入标的数（细分：新建仓 NEW / 加仓 ADD） / 调出标的数（细分：清仓 EXIT / 减仓 REDUCE） / 持续持有数
  - 未成交标的数及占比
  - 部分成交标的数及平均 fill_ratio
  - 本期单边换手率
  - 本期交易成本合计（bp）

### 10.3 日度持仓快照 🟡

调仓明细记录的是"变动"，审计和对账更需要"状态"。建议输出每日持仓快照文件 `portfolio_holdings_YYYYMMDD.csv`（至少在每个调仓周期输出一次），字段如下：

| 字段名 | 类型 | 说明 |
|--------|------|------|
| trade_date | str(YYYYMMDD) | 日期 |
| ts_code | str | 标的代码 |
| weight | float | 持仓权重 |
| shares | int | 持股数量 |
| market_value | float | 持仓市值（元） |
| industry | str | 所属行业 |
| cost_basis | float | 持仓成本价 |
| status | str | 持仓状态（ACTIVE / PENDING_EXIT），用于标识 §4.4 "待移出"标的 |

---

## 11. 测试与稳健性规范

### 11.1 最小单元测试集 🔴
至少覆盖：
- 调仓日期生成正确性
- 信号识别正确性
- 前视偏差过滤（含 DATA_AVAILABILITY_LAG）
- 权重和约束（和为1）
- 缓存自动失效与重建
- schema/coverage 校验器行为
- 交易制度约束（T+1、涨跌停、停牌）
- 组合约束触发后的权重修正
- 复权价格无异常断裂
- 停牌超限强制移出逻辑
- DELISTED_DURING_SUSPEND 结算路径正确性（§4.4）
- PENDING_EXIT 状态持仓快照正确落盘（§10.3）
- 随机种子固定后结果可复现

### 11.2 回归测试 🟡
- 固定样本区间输出关键指标阈值（年化、回撤、IR）。
- 代码改动后若指标偏离阈值，需人工复核。

### 11.3 稳健性与过拟合防控 🔴
- 必须进行子样本检验：牛/熊/震荡区间。
- 必须进行参数敏感性分析：**核心连续参数** ±20% 扰动；**离散参数**（如调仓频率）须测试相邻档位（周 ↔ 双周 ↔ 月）。
- 必须进行执行延迟检验：T+1 开盘、T+1 收盘对比。
- 必须记录训练/验证/测试时间切分，不允许时间穿越。
- 禁止依据测试集表现反复调参；若调参，必须重置切分并保留版本记录。

### 11.4 统计显著性检验 🔴
- **策略超额收益的 t 检验**：
  - 使用 Newey-West 调整的 t 统计量（考虑收益率自相关），滞后阶数 ≥ `ceil(T^(1/3))`。
  - 门槛：**t ≥ 2.0**（双尾检验，对应 p ≈ 0.046）方可认为超额收益统计显著。
  - 若检验*方向性假设*（$H_1: \alpha > 0$），可使用单尾检验，此时 t ≥ 2.0 对应 p ≈ 0.023，更为保守。须在 run_metadata 中注明所用检验类型（`test_type: "two-tailed"` 或 `"one-tailed"`）。
  - **短样本警告**：样本期不足 3 年（< 750 个交易日）时，t-stat 的可靠性下降，建议将门槛提升至 **t ≥ 2.5** 并在报告中标注"短样本"。
- **置信区间**：
  - 必须输出年化超额收益的 95% 置信区间。
  - 推荐补充 **stationary block bootstrap** 置信区间（重采样 ≥ 1000 次）作为非参数稳健性参考。
    - block 长度推荐与 Newey-West 滞后阶数一致（`ceil(T^(1/3))`）。
    - 禁止使用 i.i.d. bootstrap，因日收益率存在自相关，i.i.d. 采样会低估标准误。
    - 可选替代：circular block bootstrap。
- **多重检验调整**：
  - 若同一数据集上测试了 N > 1 个策略/参数组合，必须进行多重检验调整。
  - 推荐使用 Benjamini-Hochberg（FDR）方法；至少报告调整前后的 p-value。
  - 禁止仅报告最优参数组合的单次 p-value 而隐瞒尝试次数。

### 11.5 样本外检验 🔴
- 必须保留**至少最近 1 年**的数据作为严格的样本外测试区间（低频策略——季度及以下调仓——建议 OOS ≥ 2 年，以确保足够的调仓周期数）。
- 样本外区间的数据在策略开发和参数调优过程中**严禁使用**。
- 样本外结果须在 run_metadata 中**单独报告**：
  - `oos_start_date`：样本外起始日期
  - `oos_annual_return`：样本外年化收益
  - `oos_sharpe`：样本外 Sharpe
  - `oos_max_drawdown`：样本外最大回撤
- 样本内 vs 样本外绩效的显著差异（如 Sharpe 降幅 > 50%）须在报告中标注并解释。

### 11.6 策略衰减监控 🟡
- 必须输出**滚动 12 个月的 Sharpe / IC 时序图**，用于判断策略 alpha 是否在衰减。
- 在研究阶段建立衰减基线，实盘运行后可直接对比。
- 若滚动 12 个月 Sharpe 连续 3 个月低于 0，须在报告中标注"策略可能进入衰减期"。

### 11.7 极端区间专项检验 🟡
- 必须单独输出策略在以下极端区间的表现（若回测期覆盖）：

| 区间 | 时间段 | 特征 |
|------|--------|------|
| 2015 年股灾 | 2015-06 ~ 2015-09 | 流动性枯竭、千股跌停 |
| 2018 年去杠杆 | 2018-01 ~ 2018-12 | 单边下跌、信用风险 |
| 2020 年疫情冲击 | 2020-01 ~ 2020-03 | 外生冲击、V 型反转 |
| 2022 年 4 月封控 | 2022-03 ~ 2022-05 | 经济停摆、情绪崩溃 |
| 2024 年初微盘股崩溃 | 2024-01 ~ 2024-02 | 量化踩踏、流动性危机 |
| 2025 年（待定） | TBD | 每年 Q1 review 时评估上一年度是否有需纳入的极端区间 |

- 极端区间列表应**动态维护**，每年 review 时评估是否需新增。
- 极端区间的年化收益、最大回撤、日均换手须在报告中单独列示。
- 这些区间的表现对投委会决策和风控评估至关重要。

---

## 12. 发布门禁

### 12.1 Code Review 流程 🔴

策略代码提交发布前，必须通过人工 Code Review：

| 步骤 | 责任人 | 检查内容 |
|------|--------|----------|
| 1. 自检 | 策略作者 | 完成 §14 快速验收清单全部勾选 |
| 2. 同行评审 | **至少一名非策略作者** | 选股逻辑正确性、权重计算逻辑、PIT 合规、成本处理 |
| 3. 风控审批 | 风控负责人 | 组合约束、容量评估、尾部风险指标合理性 |
| 4. 签字存档 | 全部参与人 | 在 `code_review_sign.json` 中记录评审人、日期、结论、遗留问题 |

- Review 重点关注**自动化测试覆盖不到的逻辑**：
  - 选股条件的业务合理性（如"净利润断层"定义是否与研报一致）
  - 行业分类映射是否正确（须为历史版本）
  - 极端情况下的权重分配（如某行业仅剩 1 只股票）
- `code_review_sign.json` 纳入 run 目录存档。

**`code_review_sign.json` 最小字段定义**：

```json
{
  "review_id": "CR-2026-001",
  "strategy_name": "净利润断层_v1",
  "reviewers": [
    {"name": "张三", "role": "author", "sign_date": "2026-02-17"},
    {"name": "李四", "role": "peer_reviewer", "sign_date": "2026-02-17"},
    {"name": "王五", "role": "risk_manager", "sign_date": "2026-02-17"}
  ],
  "review_date": "2026-02-17",
  "result": "PASS",
  "conditions": [],
  "open_issues": [],
  "comments": "选股逻辑与研报一致，PIT 合规，成本处理正确"
}
```

- `result` 取值：`PASS`（通过）/ `CONDITIONAL`（有条件通过，须列明 `conditions`）/ `REJECT`（驳回）。
- `conditions` 为有条件通过时的待办项列表，须在指定日期前完成。
- `open_issues` 为已知但不阻塞发布的遗留问题。

### 12.2 自动化门禁（必须全部通过）

- 🔴 语法检查通过
- 🔴 单元测试全部通过
- 🔴 数据校验 errors = 0
- 🔴 PIT 与幸存者偏差检查通过（含公告缓冲期）
- 🔴 复权方式显式声明且校验通过
- 🔴 随机种子声明且复现性校验通过
- 🔴 缓存签名机制生效
- 🔴 回测结果可复现（相同输入 + 相同种子 = 相同输出）
- 🔴 交易可实现性检查通过（成交约束、制度约束、执行窗口）
- 🔴 超额收益 Newey-West t-stat ≥ 2.0（双尾），**或**满足以下替代条件之一：① 样本外 Sharpe > 0.5 且方向与样本内一致；② 有明确经济逻辑支撑 + Code Review 全票通过 + 风控签批同意（须在 `waiver_log.json` 中以 `priority_level = "P0"` 留痕）
- 🔴 样本外检验结果已单独报告
- 🟡 组合约束与归因报告完整
- 🟡 策略容量评估已完成
- 🔴 稳健性与过拟合检查通过
- 🔴 Code Review 签字完成

---

## 13. 研究→实盘交接规范

### 13.1 适用场景 🟡
当策略从回测阶段进入模拟盘或实盘运行时，必须完成以下交接流程。

### 13.2 信号交接 🔴

| 项目 | 要求 |
|------|------|
| 信号格式 | 必须定义标准信号输出格式（ts_code, direction, target_weight, signal_date），与交易系统对接。`direction` 须使用 §10.1 定义的 5 值枚举（NEW / ADD / HOLD / REDUCE / EXIT），禁止自定义映射 |
| 信号频率 | 明确信号生成时间点（如每日 16:00 / 调仓日 15:30 后） |
| 信号传输 | 定义传输方式（文件/API/消息队列）及重传机制 |
| 信号校验 | 交易系统接收后须校验：权重和 ≤ 1、标的在可交易池内、无重复信号 |

### 13.3 持仓初始化 🔴
- 首次上线必须定义初始持仓：空仓建仓 or 按回测最新调仓结果一次性建仓。
- 建仓过程的成交记录须与回测同格式存档。
- 若实盘持仓与回测持仓产生偏差（部分未成交），须定义**偏差处理规则**：
  - 下一交易日补单 or 放弃该标的至下次调仓。

### 13.4 收益对账 🔴
- 实盘运行后，必须每日对账：
  - 实盘日收益 vs 回测模拟日收益（同日同策略）。
  - 偏差告警阈值须分层设定：**非调仓日 ≥ 10bp**、**调仓日 ≥ 50bp** 须触发告警并记录原因。非调仓日大偏差往往指向系统性问题（复权因子差异、数据修正等），不应被高阈值掩盖。
- 对账报告格式：

| 字段 | 说明 |
|------|------|
| trade_date | 日期 |
| ret_backtest | 回测收益率 |
| ret_live | 实盘收益率 |
| diff_bps | 偏差（bp） |
| alert | 是否触发告警 |
| reason | 偏差原因（滑点/未成交/数据修正/其他） |

- 须输出**周度/月度对账汇总报告**：累计偏差、偏差分布、主要偏差来源 Top 5。

### 13.5 熔断与降级 🟡
- 必须定义实盘熔断条件（至少包含）：
  - 单日绝对亏损 > 3%
  - 单日相对基准亏损 > 2%
  - 连续 N 日对账偏差超标
  - 回撤达到预设阈值
- 熔断触发后的处理：
  - 立即停止新开仓
  - 是否平仓由人工决定
  - 必须输出熔断报告
- 以上参考值须在 config 中可配置。

### 13.6 环境一致性 🔴
- 实盘运行环境（Python 版本、依赖版本、数据源版本）必须与回测环境一致。
- 任何环境变更须重新跑回测验证结果一致后方可上线。
- 推荐使用 Docker 镜像锁定运行环境。

---

## 14. 快速验收清单

### 数据层
- [ ] 🔴 API 字段与代码预期字段一致（有 schema 校验）
- [ ] 🔴 交易日历升序
- [ ] 🔴 日期类型统一
- [ ] 🔴 关键字段覆盖率达标
- [ ] 🔴 复权方式显式声明，复权后价格无异常跳变（分板块阈值，见 §3.5）
- [ ] 🔴 行业分类使用历史时点版本
- [ ] 🔴 指数成分股使用历史时点列表
- [ ] 🟡 数据清洗规则生效，清洗日志可查
- [ ] 🟡 数据源已声明

### 回测严谨性
- [ ] 🔴 PIT 原则严格执行（含公告缓冲期 DATA_AVAILABILITY_LAG）
- [ ] 🔴 无幸存者偏差
- [ ] 🔴 基准在 config 中显式定义，所有超额/归因以此为锚
- [ ] 🔴 基准收益类型（BENCHMARK_TYPE）已声明（全收益 / 价格指数）
- [ ] 🔴 随机种子已声明，结果可复现
- [ ] 🔴 无风险利率已声明

### 交易可实现性
- [ ] 🔴 交易制度约束生效（T+1、涨跌停、停牌、停牌超限移出）
- [ ] 🔴 停牌移出卖出价假设（SUSPEND_EXIT_PRICE）已声明（§4.4）
- [ ] 🟡 闲置资金收益假设（CASH_RETURN_ASSUMPTION）已声明
- [ ] 🔴 成本模型至少含基础层与增强层，参考值已核实（低佣 7.7bp / 零售 10.2bp，印花税千0.5）
- [ ] 🟡 单笔最低佣金（MIN_COMMISSION_PER_TRADE）已配置（§5.3.1）
- [ ] 🔴 调仓执行窗口明确，执行延迟敏感性已评估
- [ ] 🔴 调仓明细 rebalance_log.csv 格式完整（含 fill_ratio）

### 缓存与性能
- [ ] 🟡 批量下载 + 本地缓存可用
- [ ] 🔴 缓存版本与签名生效，逻辑变更可自动失效重建
- [ ] 🟡 缓存过期机制生效（默认 90 天）

### 风险与归因
- [ ] 🟡 组合约束（单票/行业/换手/HHI）生效并有日志
- [ ] 🟡 归因报告（行业/选股/交易）完整，残差 ≤ ±50bp/年
- [ ] 🔴 尾部风险指标（CVaR、MaxDD Duration、月度胜率）已输出
- [ ] 🟡 策略容量评估已完成，有效容量上限已输出

### 统计与稳健性
- [ ] 🔴 Newey-West t-stat ≥ 2.0（双尾）或满足替代条件（§12.2），且 p-value 已报告
- [ ] 🔴 95% 置信区间已输出（stationary block bootstrap）
- [ ] 🔴 多参数组合已做多重检验调整（若适用）
- [ ] 🔴 稳健性与过拟合检验通过（子样本、参数扰动、执行延迟）
- [ ] 🔴 样本外检验（≥ 1 年）结果已单独报告
- [ ] 🟡 策略衰减监控（滚动 12 月 Sharpe）已输出
- [ ] 🟡 极端区间专项已输出（按 §11.7 最新清单）

### 发布与交接
- [ ] 🔴 Code Review 签字完成
- [ ] 🔴 运行产物完整可审计
- [ ] 🔴 自动化测试通过
- [ ] 🟡 研究→实盘交接文档就绪（若需上线）

---

## 15. 规范治理与持续改进

### 15.1 修订流程 🟡

- **版本命名规则**：文件名锁定主版本号（如 `量化回测代码规范_v1.5.md`），修订号（r1, r2, ...）仅在文档头部 `版本` 字段和变更日志中体现。文件名仅在主版本升级（如 v1.5 → v2.0）时变更。
- **主版本升级判定**：出现以下任一情形时须升级主版本号：P0 条目语义变更、输出字段破坏性变更（不向后兼容）、规范适用范围扩展（如新增衍生品覆盖）。

```text
修订草案（策略/风控）
  → 团队内评审（≥ 2 人书面反馈）
  → 量化 Lead 签批
  → 更新 changelog + 全员通知
  → 归档历史版本
```

### 15.2 定期 Review 🟡
- 每季度末组织一次规范 review 会议。
- 议题包括：新增条目、删减过时条目、P 级调整、豁免回顾。
- 会议纪要纳入规范 Git 仓库。

### 15.3 回测结果有效期 🟡
- 通过门禁的回测结果在 **6 个月** 后自动过期，须重新验证后方可继续使用。
- **重新验证定义**：使用最新数据**全量重跑**回测，config 参数不得修改（否则视为新策略，须走完整审批流程）。仅允许更新数据区间终点。
- 过期的回测结果不得作为实盘决策依据。
- 目的：市场结构变化可能导致历史上有效的策略失效。

---

## 附录 A：术语表

| 术语 | 英文全称 | 定义 |
|------|----------|------|
| PIT | Point-In-Time | 时点数据原则：仅使用在某一时点已公开可得的数据，避免用到未来才发布的信息 |
| 前视偏差 | Look-Ahead Bias | 在回测中使用了回测时点尚未可得的信息（如未来公告数据），导致回测结果虚高 |
| 幸存者偏差 | Survivorship Bias | 仅使用当前存续的标的回溯历史，遗漏已退市/摘牌标的，导致结果偏乐观 |
| CVaR | Conditional Value-at-Risk | 条件在险价值，衡量损失超过 VaR 后的平均损失程度（也称 Expected Shortfall, ES） |
| MaxDD | Maximum Drawdown | 最大回撤，从净值峰值到后续最低点的最大跌幅 |
| HHI | Herfindahl-Hirschman Index | 赫芬达尔指数，$\sum w_i^2$，衡量组合集中度。等权 100 只股票时 HHI=0.01，集中持有 1 只时 HHI=1.0 |
| TWAP | Time-Weighted Average Price | 时间加权平均价格，在指定时间窗口内按时间等权的平均成交价 |
| VWAP | Volume-Weighted Average Price | 成交量加权平均价格，在指定时间窗口内按成交量加权的平均成交价 |
| Sharpe Ratio | Sharpe Ratio | 夏普比率，$\frac{R_p - R_f}{\sigma_p}$，衡量单位风险的超额回报 |
| IR | Information Ratio | 信息比率，$\frac{R_p - R_b}{\sigma_{p-b}}$，衡量相对基准的风险调整后超额收益 |
| IC | Information Coefficient | 信息系数，因子值与未来收益的截面 Spearman 秩相关系数 |
| ICIR | IC Information Ratio | IC 的均值 / IC 的标准差，衡量因子预测能力的稳定性 |
| Alpha | Jensen's Alpha | 策略收益中不能被市场风险因子解释的部分 |
| Newey-West | Newey-West Estimator | 一种 HAC（异方差自相关一致）协方差估计方法，修正 OLS 标准误以应对时间序列中的自相关和异方差 |
| BH-FDR | Benjamini-Hochberg False Discovery Rate | 一种多重检验调整方法，控制错误发现率而非族错误率，比 Bonferroni 更温和 |
| Block Bootstrap | Stationary Block Bootstrap | 一种保留时间序列依赖结构的重采样方法，将数据按随机长度的连续块重采样，适用于自相关数据 |
| 复权 / 复权因子 | Price Adjustment / Adjustment Factor | 修正送转、配股、拆合股等公司行动对股价的影响，使历史价格序列可比 |
| T+1 | Trade + 1 Day | A 股交易制度：当日买入的股票，次日起方可卖出 |
| ST | Special Treatment | 特别处理，对财务或经营异常的上市公司实施的交易限制（涨跌幅收窄至 ±5%） |
| bp | Basis Point | 基点，1bp = 0.01% = 0.0001 |
| 冲击成本 | Market Impact Cost | 因自身交易行为引起的不利价格变动，通常与成交占比、波动率正相关 |
| 滑点 | Slippage | 预期成交价与实际成交价之间的差异，由流动性不足、价格波动等造成 |
| PnL Ratio | Profit/Loss Ratio | 盈亏比，平均盈利交易收益 / 平均亏损交易损失的绝对值 |
| Almgren-Chriss | Almgren-Chriss Model | 最优执行模型，在最小化冲击成本和时间风险之间取得平衡 |
| OOS | Out-of-Sample | 样本外，策略开发和参数调优过程中未使用的数据区间 |
| SUSPEND_EXIT | Suspend Exit | 停牌超限强制移出后正常复牌卖出的 fail_reason 标记（见 §4.4） |
| DELISTED_DURING_SUSPEND | Delisted During Suspension | 停牌期间触发退市的 fail_reason 标记，结算规则见 §4.4 |

---

## 附录 B：豁免记录模板（P0 / P1 通用）

当 P0 或 P1 条目需要临时豁免时，须填写以下记录并纳入 run 目录存档（文件名 `waiver_log.json`）：

```json
{
  "waivers": [
    {
      "waiver_id": "W001",
      "priority_level": "P1",
      "rule_ref": "§8.4",
      "rule_desc": "策略容量评估",
      "reason": "当前策略仅用于研究验证，不涉及实盘资金分配，容量评估暂缓",
      "impact_assessment": "不影响回测正确性，仅影响实盘可行性评估",
      "applicant": "张三",
      "approver": "李四（量化 Lead）",
      "apply_date": "2026-02-17",
      "expiry_date": "2026-05-17",
      "status": "active"
    }
  ]
}
```

**关键字段说明**：
- `priority_level`：被豁免条目的级别（`"P0"` 或 `"P1"`）。**P0 豁免须额外满足**：Code Review 全票通过 + 风控负责人签批，且 `expiry_date` 不得超过 1 个月
- `rule_ref`：被豁免的规范条目编号
- `reason`：豁免原因（须具体，不得写"暂不需要"）
- `impact_assessment`：豁免对结果可信度的影响评估
- `expiry_date`：豁免到期日（P1 最长 3 个月，P0 最长 1 个月），到期后须重新评估
- `approver`：审批人（P1 须为量化 Lead 或风控负责人；P0 须为风控负责人）
